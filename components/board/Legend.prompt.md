Bottom-right key explaining what each node fill means. Include one on every board that uses more than one tier.

```jsx
<Legend items={[
  { tier: "primary", label: "Main screens" },
  { tier: "secondary", label: "On-screen contents" },
  { tier: "content", label: "Content screens" },
]} />
```

Order items by hierarchy depth, outermost first. Labels are two or three words, no sentences.
