A mono note pinned near the thing it explains — the only place on a board where sentence-case prose is allowed.

```jsx
<Annotation width={220}>deep link lands here when the invite is opened cold</Annotation>
```

`tone="strong"` for a note that must be read (a blocker, a decision needed). Never more than two lines.
